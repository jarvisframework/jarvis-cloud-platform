server:
  port: 10079
  servlet:
    context-path: /admin
spring:
  application:
    name: jarvis-admin
  security:
    user:
      name: admin
      password: 123456
  #cloud:
  #  nacos:
  #    discovery:
  #      metadata:
  #        user.name: ${spring.security.user.name}
  #        user.password: ${spring.security.user.password}
